# Banco de dados
# Local:
DATABASE_URL=postgresql://truco:truco_dev@localhost:5432/truco?schema=public
# Supabase (Connect > Session pooler; troque [SENHA] pela senha do banco):
# DATABASE_URL=postgresql://postgres.[ref]:[SENHA]@aws-1-sa-east-1.pooler.supabase.com:5432/postgres

# Redis (cache, presença, filas de matchmaking, adapter Socket.IO)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=troque-este-segredo
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=troque-este-tambem
JWT_REFRESH_EXPIRES_IN=30d

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# E-mail (verificação de conta e recuperação de senha)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Truco Paulista <no-reply@trucopaulista.app>

# App
PORT=3001
WEB_ORIGIN=http://localhost:3000
